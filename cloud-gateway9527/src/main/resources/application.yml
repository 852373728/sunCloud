server:
  port: 9527


spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  application:
    name: cloud-gateway
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
        prefer-ip-address: false

